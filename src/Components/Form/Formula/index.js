const Formula = () => {
  const bmiFormula = (
    <>
      Formula for BMI = weight (kg) / height (m)<sup>2</sup>
    </>
  );

  return (
    <div>
      <p className="paragraph">{bmiFormula}</p>
    </div>
  );
};

export default Formula;
